# Core runtime dependencies.
# On Linux these will pull the PyTorch CPU wheel automatically.
# For GPU support, install torch separately:
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch
torch>=2.0
numpy>=1.24
scipy>=1.10
