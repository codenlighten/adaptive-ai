# Development / reproduction dependencies (in addition to requirements.txt).
-r requirements.txt

# Test runner
pytest>=7.0

# Plotting (only needed to render figures from experiments)
matplotlib>=3.6

# Optional acceleration for the multiply-free matmul kernel
numba>=0.59

# External dataset for the digits classification experiment
scikit-learn>=1.3

# Hardware synthesis (Yosys + nextpnr-ice40 via WASM)
yowasp-yosys>=0.65
yowasp-nextpnr-ice40>=0.10

# Build + release tooling
build>=1.0
twine>=4.0
